# Synthetic fixtures

> Synthetic illustrative data generated for demonstration. NOT real agency data and NOT derived from any proprietary or employer source.

SD-MAC (Sector-Wide Deployable Modular Analytics Commons) synthetic fixtures: sample `module_manifest` records (validation_status: draft) used by the schema-validation demo and the SD-MAC /v1/modules endpoint. Maintainer handles and documentation URLs are intentionally preserved as `<JING_WEN_TO_FILL: ...>` placeholders.
