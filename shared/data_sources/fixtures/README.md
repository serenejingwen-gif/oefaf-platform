# Synthetic fixtures

> Synthetic illustrative data generated for demonstration. NOT real agency data and NOT derived from any proprietary or employer source.

This directory and all of its subdirectories contain synthetic fixtures used to make the oefaf-platform codebase runnable offline for demonstration. Subtrees: `gea/`, `cricat/`, `sdmac/`.

- The data was generated deterministically (seed=42) by `shared/utilities/synthetic_data.py`.
- Provider/source names appearing in fixtures are public-source category labels only.
- All URLs are illustrative `example.org` placeholders or `<JING_WEN_TO_FILL: ...>` placeholders — they are not real endpoints.
- No proprietary or employer content of any kind is present.
